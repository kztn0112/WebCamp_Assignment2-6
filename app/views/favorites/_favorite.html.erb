

<% if book.favorited_by?(current_user) %>
 <td><%= link_to book_favorites_path(book), method: :delete ,class:"favorite-btn"  do %>
   ♥<%= book.favorites.count %>
   <% end %></td>
<% else %>
 <td><%= link_to book_favorites_path(book), method: :post ,class:"no-favorite-btn" do %>
   ♡<%= book.favorites.count %>
   <% end %></td>
<% end %>

<%
=begin
%>
<% if book.favorited_by?(current_user) %>
 <td><%= link_to book_favorites_path(book), method: :delete ,class:"favorite-btn" ,remote :true do %>
   ♥<%= book.favorites.count %>
   <% end %></td>
<% else %>
 <td><%= link_to book_favorites_path(book), method: :post ,class:"no-favorite-btn" ,remote :true do %>
   ♡<%= book.favorites.count %>
   <% end %></td>
<% end %>
<%
=end
%>
